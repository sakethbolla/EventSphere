apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: prod
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - port: 4004
    targetPort: 4004
    protocol: TCP
    name: http
