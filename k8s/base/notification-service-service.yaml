apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: prod
  labels:
    app: notification-service
    app.kubernetes.io/part-of: eventsphere
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - port: 4004
    targetPort: 4004
    protocol: TCP
    name: http
